import java.util.ArrayList;
import java.util.Scanner;
import java.net.URL;

public class URLReader {
	/* 
	 * reads the data
	 * idk what i need to do with the arrayList part of it
	public static ArrayList<Player> URLReader(String adr){
		Scanner sc = new Scanner(System.in);
		System.out.print("Enter url: ");
		String line;
		adr = sc.nextLine();
		try {
			URL link = new URL(adr);
			Scanner linkSC = new Scanner(link.openStream());
			while (linkSC.hasNextLine()) {
				line = linkSC.nextLine();
				System.out.println(line);
			}
			linkSC.close();
		} catch (Exception ex) {
			System.out.println("Something went wrong.");
			ex.printStackTrace();
		}
		*/
}
